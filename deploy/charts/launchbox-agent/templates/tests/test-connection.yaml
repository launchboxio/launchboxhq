apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "launchbox-agent.fullname" . }}-test-connection"
  labels:
    {{- include "launchbox-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "launchbox-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
